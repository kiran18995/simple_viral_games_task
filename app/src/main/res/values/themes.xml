<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleViralGamesTask" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/button_color</item>
    </style>

    <style name="Base.Theme.SimpleViralGamesTask.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/button_color</item>
    </style>

    <style name="Theme.SimpleViralGamesTask" parent="Base.Theme.SimpleViralGamesTask" />
</resources>